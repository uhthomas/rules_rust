# TODO: Rename file?

CRATE_INDEX = "@{crate_index}"

# Label for the configuration file.
CONFIG = "{config}"

# Absolute path to the lockfile, as it needs to be overwritten.
LOCKFILE_PATH = "{lockfile_path}"

# TODO: Maybe json.decode("{}")?
MANIFEST = {manifest}

# Label for the splicing manifest file.
SPLICING_MANIFEST = "{splicing_manifest}"
