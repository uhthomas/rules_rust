package(default_visibility = ["//:__subpackages__"])

exports_files(glob(["**"]))

filegroup(
    name = "bzl_srcs",
    srcs = glob(["*.bzl"]),
)

filegroup(
    name = "distro",
    srcs = glob(["*.bzl"]) + ["BUILD.bazel"],
)
